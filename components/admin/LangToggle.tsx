'use client'

// The admin toggle IS the member toggle — one control, one setting. Kept as a
// re-export so the admin top bar's import does not have to change.
export { default } from '@/components/LangToggle'
